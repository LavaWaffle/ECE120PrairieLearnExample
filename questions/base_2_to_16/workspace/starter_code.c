#include <stdio.h>

// Convert from base 2 to base 16
int main() {
    int result = 0;
    int power = 1 << 7; // 128 (MSB value of a 8 bit unsigned binary)

    while (1) {
        char nextBit;
        scanf("%c", &nextBit);

        // Your code here!

    }

    printf("%X", result);

    // Keep this return 0;
    return 0;
}