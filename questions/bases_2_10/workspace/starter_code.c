#include <stdio.h>

int main() {
    // Grab first two inputs as integers
    int length, width;
    printf("Enter the length: \n");
    scanf("%d", &length);
    printf("Enter the width: \n");
    scanf("%d", &width);
    return 0;
}