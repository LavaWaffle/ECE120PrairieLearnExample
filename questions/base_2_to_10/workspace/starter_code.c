#include <stdio.h>

// Convert from base 2 to base 10
int main() {
    int result = 0;
    int power = 1 << 5; // 32 (MSB value of a 6 bit unsigned binary)

    while (1) {
        char nextBit;
        scanf("%c", &nextBit);

        // Your code here!

    }

    printf("%d", result);

    // Keep this return 0;
    return 0;
}